import React from "react";
import css from "./style.module.css"
import {  Box, Container } from "@mui/material";

const ProfilePage = () => {
    return (
        <Container className={css.Container} >
        <Box className={css.Rec}>
            ProfilePage exists
        </Box>
        </Container>
    )
}

export default ProfilePage;

